(function($) {
    "use strict";


    var forms = require('./modules/forms');
    var slider = require('./modules/slider');
    var map = require('./modules/map');

    forms();
    slider();
    map();
})(jQuery);